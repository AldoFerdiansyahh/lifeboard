import{escapeHTML,formatCurrency,formatShortDate}from'../utils.js';
export const renderTransactionCard=t=>`<article class="list-item" data-id="${t.id}"><div class="list-main"><div class="list-title">${escapeHTML(t.category)}</div><div class="list-meta"><span>${formatShortDate(t.date)}</span>${t.note?`<span>${escapeHTML(t.note)}</span>`:''}</div></div><strong class="amount ${t.type}">${t.type==='expense'?'−':'+'}${formatCurrency(t.amount)}</strong><div class="item-actions"><button data-edit aria-label="Edit transaksi">✎</button><button data-delete aria-label="Hapus transaksi">×</button></div></article>`;
