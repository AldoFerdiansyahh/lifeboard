import{state}from'../state.js';import{escapeHTML,debounce,formatCurrency}from'../utils.js';import{showModal,closeModal}from'./modal.js';import{router}from'../router.js';
export function openGlobalSearch(){const box=document.createElement('div');box.innerHTML=`<input class="input" type="search" placeholder="Search tasks, schedule, notes, transactions…" aria-label="Global search"><div data-results><div class="empty"><p>Type to search your local data.</p></div></div>`;showModal({title:'Search LIFEBOARD',content:box,className:'search-overlay'});const input=box.querySelector('input'),results=box.querySelector('[data-results]');input.oninput=debounce(()=>{const q=input.value.trim().toLowerCase();if(!q){results.innerHTML='<div class="empty"><p>Type to search your local data.</p></div>';return}const groups=[['Tasks','tasks','tasks',x=>x.title+' '+x.description],['Schedule','events','schedule',x=>x.title+' '+x.location],['Notes','notes','notes',x=>x.title+' '+x.content],['Transactions','transactions','money',x=>x.category+' '+x.note+' '+formatCurrency(x.amount)]];results.innerHTML=groups.map(([label,key,route,text])=>{const hits=state.data[key].filter(x=>text(x).toLowerCase().includes(q)).slice(0,5);return hits.length?`<section class="search-group"><h3>${label}</h3>${hits.map(x=>`<button class="list-item" style="width:100%;text-align:left" data-go="${route}"><span class="list-title">${escapeHTML(x.title||x.category)}</span></button>`).join('')}</section>`:''}).join('')||'<div class="empty"><p>No results found.</p></div>';results.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>{closeModal(true);router.go(b.dataset.go)})},180)}
