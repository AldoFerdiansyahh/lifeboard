import{escapeHTML,formatShortDate}from'../utils.js';
export const renderTaskCard=t=>`<article class="list-item ${t.status==='completed'?'completed':''} priority-${t.priority}" data-id="${t.id}"><input class="check" type="checkbox" data-task-toggle aria-label="Mark ${escapeHTML(t.title)} as completed" ${t.status==='completed'?'checked':''}><div class="list-main"><div class="list-title">${escapeHTML(t.title)}</div><div class="list-meta"><span>${escapeHTML(t.category)}</span>${t.dueDate?`<span>${formatShortDate(t.dueDate)} ${t.dueTime||''}</span>`:''}${t.status!=='completed'&&t.dueDate&&t.dueDate<new Date().toLocaleDateString('en-CA')?'<span class="badge text-danger">OVERDUE</span>':''}<span class="badge">${escapeHTML(t.priority)}</span></div></div><div class="item-actions"><button data-edit aria-label="Edit task">✎</button><button data-delete aria-label="Delete task">×</button></div></article>`;
