import{state}from'../state.js';import{todayISO,getGreeting,formatDate,formatCurrency,formatTime,monthKey,escapeHTML}from'../utils.js';import{openTask}from'./tasks.js';import{openEvent}from'./schedule.js';import{openTransaction}from'./finance.js';import{openNote}from'./notes.js';
export function renderDashboard(){const d=state.data,today=todayISO(),month=monthKey(today),tasks=d.tasks.filter(x=>x.dueDate===today||!x.dueDate),done=d.tasks.filter(x=>x.status==='completed'),events=d.events.filter(x=>x.date===today),upcoming=d.events.filter(x=>x.date>=today).sort((a,b)=>(a.date+a.startTime).localeCompare(b.date+b.startTime)).slice(0,5),txMonth=d.transactions.filter(x=>monthKey(x.date)===month),income=txMonth.filter(x=>x.type==='income').reduce((s,x)=>s+x.amount,0),expense=txMonth.filter(x=>x.type==='expense').reduce((s,x)=>s+x.amount,0),todayExpense=d.transactions.filter(x=>x.date===today&&x.type==='expense').reduce((s,x)=>s+x.amount,0),priority=d.tasks.filter(x=>x.status!=='completed'&&['urgent','high'].includes(x.priority)).slice(0,5),pct=tasks.length?Math.round(tasks.filter(x=>x.status==='completed').length/tasks.length*100):0,name=d.settings.firstName?.trim();document.querySelector('#main-content').innerHTML=`<section class="page"><header><p class="eyebrow">${formatDate(today,{weekday:'long'})}</p><h2 style="font-size:clamp(1.7rem,4vw,2.7rem);margin-top:.3rem">${getGreeting()}${name?`, ${escapeHTML(name)}`:''}</h2><p class="muted">Here’s your day at a glance.</p></header><div class="grid overview-grid"><article class="card metric"><span class="metric-icon">□</span><strong class="metric-value">${events.length}</strong><span class="metric-label">Events today</span></article><article class="card metric"><span class="metric-icon">✓</span><strong class="metric-value">${tasks.length}</strong><span class="metric-label">Tasks today</span></article><article class="card metric"><span class="metric-icon">●</span><strong class="metric-value">${done.length}</strong><span class="metric-label">Tasks completed</span></article><article class="card metric"><span class="metric-icon">₨</span><strong class="metric-value">${formatCurrency(todayExpense)}</strong><span class="metric-label">Spent today</span></article></div><div class="dashboard-grid"><article class="card section-card"><div class="section-head"><h2>Upcoming schedule</h2><a class="muted" href="#/schedule">View all</a></div><div class="list">${upcoming.length?upcoming.map(e=>`<div class="list-item"><strong class="nowrap">${formatTime(e.startTime)}</strong><div class="list-main"><div class="list-title">${escapeHTML(e.title)}</div><div class="list-meta">${formatDate(e.date,{year:undefined})} · ${escapeHTML(e.category)}</div></div></div>`).join(''):'<div class="empty"><p>No upcoming schedule.</p></div>'}</div></article><article class="card section-card"><h2>Daily progress</h2><strong class="metric-value">${tasks.filter(x=>x.status==='completed').length}/${tasks.length} completed</strong><div class="progress"><span style="width:${pct}%"></span></div><small>${pct}% of today’s tasks</small></article><article class="card section-card"><div class="section-head"><h2>Priority tasks</h2><a class="muted" href="#/tasks">View all</a></div><div class="list">${priority.length?priority.map(t=>`<div class="list-item priority-${t.priority}"><div class="list-main"><div class="list-title">${escapeHTML(t.title)}</div><div class="list-meta">${escapeHTML(t.category)} · ${t.priority}</div></div></div>`).join(''):'<div class="empty"><p>No priority tasks.</p></div>'}</div></article><article class="card section-card"><h2>Finance snapshot</h2><div class="list"><div class="section-head"><span>Income</span><strong class="text-success">${formatCurrency(income)}</strong></div><div class="section-head"><span>Expense</span><strong class="text-danger">${formatCurrency(expense)}</strong></div><div class="section-head"><span>Balance</span><strong>${formatCurrency(income-expense)}</strong></div></div></article><article class="card section-card span-full"><h2>Quick actions</h2><div class="quick-actions"><button class="quick-action" data-quick="task">+ Task</button><button class="quick-action" data-quick="event">+ Schedule</button><button class="quick-action" data-quick="income">+ Income</button><button class="quick-action" data-quick="expense">+ Expense</button><button class="quick-action" data-quick="note">+ Note</button></div></article></div></section>`;document.querySelectorAll('[data-quick]').forEach(b=>b.onclick=()=>({task:()=>openTask({}),event:()=>openEvent({}),income:()=>openTransaction({type:'income'}),expense:()=>openTransaction({type:'expense'}),note:()=>openNote({})}[b.dataset.quick]()))}
